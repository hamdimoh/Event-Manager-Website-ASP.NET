@model MitwirkenderVM

@{

    List<SelectListItem> l1 = new List<SelectListItem>();
    l1.Add(new SelectListItem() { Text = "Keine", Value = "Keine" });
    l1.Add(new SelectListItem() { Text = "Fleisch", Value = "Fleisch" });
    l1.Add(new SelectListItem() { Text = "Vegan", Value = "Vegan" });
    l1.Add(new SelectListItem() { Text = "Vegetarian", Value = "Vegetarian" });

}
<div class="card shadow border-0 my-4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2"> @(Model.Mitwirkende.Id != 0 ? "Mitwirkende bearbeiten" : "Neuen Mitwirkenden hinzufügen") </h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post" class="row" enctype="multipart/form-data">
            <input asp-for="Mitwirkende.Id" hidden />
            <div class="row">
                <div class="col-12">
                    <div class="border p-3">
                        @*<div asp-validation-summary="ModelOnly"></div>*@
                        <div class="form-floating py-2 col-12">
                            <input asp-for="Mitwirkende.Name" class="form-control border-0 shadow" />
                            <label asp-for="Mitwirkende.Name" class="ms-2"></label>
                            <span asp-validation-for="Mitwirkende.Name" class="text-danger"></span>
                        </div>

                        <div class="form-floating py-2 col-12">
                            <input asp-for="Mitwirkende.Email" class="form-control border-0 shadow" />
                            <label asp-for="Mitwirkende.Email" class="ms-2"></label>
                            <span asp-validation-for="Mitwirkende.Email" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <input asp-for="Mitwirkende.Aufgaben" class="form-control border-0 shadow" />
                            <label asp-for="Mitwirkende.Aufgaben" class="ms-2"></label>
                            <span asp-validation-for="Mitwirkende.Aufgaben" class="text-danger"></span>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <select asp-for="Mitwirkende.Essen" asp-items="@l1" class="form-select border-0 shadow">
                                <label asp-for="Mitwirkende.Essen" class="ms-2">Essen</label>
                                <option disabled selected>--Essen Auswählen--</option>
                            </select>
                        </div>
                        <div class="form-floating py-2 col-12">
                            <input asp-for="Mitwirkende.Allergien" class="form-control border-0 shadow" />
                            <label asp-for="Mitwirkende.Allergien" class="ms-2"></label>
                            <span asp-validation-for="Mitwirkende.Allergien" class="text-danger"></span>
                        </div>
                        
                        <div class="form-floating py-2 col-12">
                            <select asp-for="@Model.Mitwirkende.VeranstaltungId" asp-items="@Model.VeranstaltungList" class="form-select border-0 shadow">
                                <option disabled selected>--Veranstaltung--</option>
                            </select>
                            <label asp-for="Mitwirkende.VeranstaltungId" class="ms-2">Veranstaltung</label>
                            <span asp-validation-for="Mitwirkende.VeranstaltungId" class="text-danger"></span>
                        </div>



                        <div class="row pt-2">
                            <div class="col-6 col-md-3">
                                @if (Model.Mitwirkende.Id != 0)
                                {
                                    <button type="submit" class="btn btn-primary form-control">Aktualisieren</button>
                                }
                                else
                                {
                                    <button type="submit" class="btn btn-primary form-control">Einfügen</button>
                                }
                            </div>
                            <div class="col-6 col-md-3">
                                <a asp-controller="A_Mitwirkende" asp-action="Index" class="btn btn-outline-primary border  form-control">
                                    Zurück
                                </a>
                            </div>
                        </div>


                    </div>
                </div>

            </div>

        </form>
    </div>
</div>
@section Scripts {
  

    <script>
        $(document).ready(function () {
            if ($('#mov_type').length) {
                $('#mov_type').val(localStorage.getItem("mov_type"));
            }
        });
    </script>
}